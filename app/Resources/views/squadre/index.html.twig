<h1>Squadres list</h1>
{#<div class="panel-body">
    {{ stampaTabella(persones) }}#}
    <div class="row">
        <div class="col-sm-12">
            <div class="row">
                <div class="col-sm-12">
                    <table class="table table-bordered table-striped">
                        <thead>
                        <tr>
                            <th>Nome</th>
                            <th>Descrizione</th>
                            <th>Azione</th>
                        </tr>
                        </thead>
                        <tbody>
                        {#{{ dump() }}#}
                        {% for pippe in pippes %}
                            <tr>
                                <td>{{ pippe.nome }}</td>
                                <td>{{ pippe.descrizione }}</td>
                                {#<td>#}
                                    {#{% if pippe.idSquadra !=null %}#}
                                        {#<a href="#"#}
                                           {#onclick="loadFormIntoModal(Routing.generate('squadre_edit',{id:{{ pippe.id }}}));"#}
                                        {#>{{ pippe.descrizione }} </a>#}
                                        {#<br># {{  pippe.idSquadra.elencoPersone | length}}#}
                                    {#{% endif %}#}
                                {#</td>#}
                                <td>
                                    <ul>
                                        <li>
                                            <a href="#"
                                               onclick="loadFormIntoModal(Routing.generate('squadre_edit',{id:{{ pippe.id }}}));"
                                            >Modifica</a>
                                        </li>
                                        {#<li>#}
                                            {#<a href="#"#}
                                               {#onclick="loadConfirmModal(Routing.generate('persone_delete',{id:{{ persona.id }}}))"#}
                                            {#>cancella</a>#}
                                        {#</li>#}
                                    </ul>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
<ul>
    <li>
        <a href="#"
           onclick="loadFormIntoModal(Routing.generate('squadre_new'));"
        >Nuovo</a>
    </li>
</ul>


